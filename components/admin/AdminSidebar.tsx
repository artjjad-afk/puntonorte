// Este componente está deprecado — la navegación del admin
// la maneja NavDock (barra inferior flotante).
// Se mantiene para no romper imports existentes.
export function AdminSidebar() {
  return null
}
