'use client'

/* ────────────────────────────────────────────────────────────────────────────
   DEPRECADO — reemplazado por <NavDock /> montado en <ConsoleShell />.
   La navegación de la consola es ahora el muelle flotante inferior.
   Se conserva como componente no-op para no tocar las páginas que todavía lo
   importan; se retirará cuando cada página se migre al nuevo lenguaje visual.
   ──────────────────────────────────────────────────────────────────────────── */
export function AdminSidebar() {
  return null
}
